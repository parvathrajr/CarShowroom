{
  "name": "luxora-motors",
  "version": "1.0.0",
  "description": "Luxora Motors — a luxury car showroom built on the MERN stack (MongoDB, Express, React, Node).",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "tables": "npm run tables --prefix server",
    "dev": "concurrently -n API,WEB -c yellow,cyan \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
